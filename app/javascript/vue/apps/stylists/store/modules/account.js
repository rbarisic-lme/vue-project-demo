import api from '@/api/stylist'
import axios from 'axios'

// initial state
// shape: [{ id, quantity }]
const state = () => ({
  profilePicture: '',
})

// getters
const getters = {
  
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}